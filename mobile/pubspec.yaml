name: truthlens_mobile
description: TruthLens AI - Multimodal Fake News Detection Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.2
  image_picker: ^1.0.5
  flutter_spinkit: ^5.2.0
  url_launcher: ^6.2.2
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
